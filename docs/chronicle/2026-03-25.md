# 2026-03-25 Chronicle

## Index
| 代号 | 模型 | 说明 |
|------|------|------|
| opus-0325a | opus-4.6 | hdmap.live 电台功能线上冒烟测试 |
| codex-0325b | gpt-5 | Signal-Map 学生向尽调 memo 落盘 |

---

### [10:04] Turn 1 `opus-0325a`
**用户**：测试 hdmap.live 线上电台功能（首页、playlist API、announce API）
**执行**：curl 冒烟测试 3 个端点
**结果**：
- 首页 → 200 PASS
- Playlist API → 200，返回 5 首 morning tracks（Komiku 系列），PASS
- Announce API → 200，text 非空（动态播报校园活动），audio 字段包含 ~1MB base64 WAV 数据，但 **audioUrl 字段不存在**（API 返回的是 `audio` 而非 `audioUrl`）
**关键发现**：announce 端点返回内联 base64 音频而非 URL，字段名为 `audio` 不是 `audioUrl`

---

### [13:31] Turn 2 `codex-0325b`
**用户**：执行 Signal-Map 深度调研计划，产出面向学生用户的对外故事型尽调 memo
**执行**：
- 系统阅读 README、STATUS、CLAUDE、radio 设计文档与首页/地图/评论/投稿/admin/ingest/radio 相关源码
- 对 hdmap.live 首页、playlist API、announce API 做线上核验
- 补充 UNC 官方资料、Heel Life、UNC Events Calendar、Hello Heels/CarolinaGO、GroupMe Campus、Discord Student Hubs、The Daily Tar Heel 等外部来源
- 新建 `docs/research/2026-03-25-signal-map-student-facing-dd.md`
**结果**：
- 形成一份可执行 memo，明确回答“它是什么、学生为什么会重复打开、比替代方案强在哪里、距离大讲特讲还差哪几步”
- 将结论分成“叙事调整”和“产品补课”两类，并附竞品表、风险表、30 天实验清单
- 明确当前最可信主楔子是“地图化发现 + 多源聚合”，而不是 AI 电台或社交网络叙事
**关键决策**：
- 对外故事采用“校园本地信息网络”定义，但仅以 discovery layer 为主，不夸张宣称已形成成熟社区网络
- 把 74% 建筑匹配率、daily cron、移动端仍偏桌面、缺自动化测试列为削弱故事可信度的核心风险

---

### [13:46] Turn 3 `codex-0325b`
**用户**：追问后台 dashboard 在哪里，希望直接知道访问入口
**执行**：
- 核对 `src/app/admin/page.tsx` 的路由与前端取 token 逻辑
- 核对 `src/app/api/admin/stats/route.ts` 的服务端鉴权方式
**结果**：
- 确认后台页面路由是 `/admin`
- 确认访问方式必须带 `?token=<ADMIN_TOKEN>`
- 确认缺 token 会显示 `Missing ?token= parameter`，token 不对会返回 `401 Unauthorized`
**关键决策**：
- 直接回用户“路径 + 参数 + 环境变量 + 典型拦截原因”，不额外展开无关实现细节

---

### [13:50] Turn 4 `codex-0325b`
**用户**：追问自己的 token 在哪里
**执行**：
- 搜索 `ADMIN_TOKEN` 在仓库中的引用
- 检查 `.env` 与 `.env.example`
- 检查当前 shell 是否额外注入 `ADMIN_TOKEN`
**结果**：
- 本地配置位置在 `.env` 第 3 行
- 当前值仍是占位字符串 `change-me-to-a-secret`，不是已设置好的真实后台密钥
- 当前 shell 环境未注入 `ADMIN_TOKEN`
**关键决策**：
- 明确区分“本地仓库里的占位值”和“线上部署环境里可能另有真实值”，避免误把占位值当成生产凭据
